class hedge{
    constructor(x, y) {
    
      this.x = x;
      this.y = y;
      this.path = path;
      this.imageWidth = 150;
      this.imageHeight = 200;
      this.myImage = loadImage(this.path);
  
    }
  
   

    // use color instead of fill
    draw()
    {
      image(img, 0, 0);

    }
}